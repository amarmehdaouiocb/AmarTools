# Whisper Prompt - Vocabulaire technique
# Ajoutez vos termes ici, un par ligne ou séparés par des virgules
# Les lignes commençant par # sont ignorées

# Git
git commit, git push, git pull, git merge, git branch, git checkout, git stash, git rebase, pull request, merge request

# Dev
API, frontend, backend, endpoint, middleware, repository, codebase, refactor, debug, deploy, build, test

# Outils
Claude, Claude Code, PowerShell, terminal, npm, pnpm, bun, node, Python, TypeScript, JavaScript

# Frameworks
React, Next.js, Vue, Supabase, Prisma, Tailwind

# Commandes Unix
cd, ls, cat, grep, find, mkdir, rm, mv, cp

# Commandes Windows
tasklist, taskkill, wmic, reg query, HKCU, HKLM, EncodedCommand

# STT
stt, sttt, sttstop, sttdaemon, sttstatus, stt_tray, stt_gui
faster-whisper, CTranslate2, Whisper, transcription

# Aliases PowerShell
gs, gp, gf, gl, gd, gds, ll, ff
devstop, devlist, goodnight

# Hardware & Mémoire
RAM, VRAM, GPU, CUDA, RTX, RTX 5090, cpu_threads, num_workers, float16, int8

# Applications Windows
Ollama, NordVPN, SteelSeries, WidgetBoard, OneDrive, Docker Desktop, pythonw

# Projets (dossiers SaaS)
SaaS, AmarTools, BoatAcademy, Fidelya, Fidelya-api, Fidelya-claude, Fidelya-codex
landingpage-fidelya, OnMangeQuoi, pronos-foot, scraper-immo, site-radi, StellarWave
